url: "https://api.oeg-kraken.energy/v1/graphql/"
method: post
content_type: "application/json"
headers:
  Authorization: "{{ token }}"
payload: >
  {
    "query": "mutation { createElectricityMeterReadings(input: { accountNumber: \"{{ account_number }}\", readingDate: \"{{ now().strftime('%Y-%m-%dT%H:%M:%S%z') }}\", meterId: \"{{ meter_id }}\", readings: { value: \"{{ reading }}\", registerObisCode: \"{{ obis_code }}\" } }) { numberOfReadingsCreated } }"
  }
